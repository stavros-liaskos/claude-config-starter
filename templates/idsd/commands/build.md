---
description: Intent-driven development — run the build loop for an intent, then write a merge summary to intents/<slug>/build.md.
argument-hint: [intent slug or title]
---

Read `.claude/skills/build/SKILL.md` for the full methodology, then run it with: $ARGUMENTS
